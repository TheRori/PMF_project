version: "3"

services:

  server:

    build: server/

    
    ports:
      - 5000:5000
      
      
  client:

    build: client/
    
    volumes:
        - ./server:/server
    
    ports:
      - 8080:8080


    depends_on:
      - server